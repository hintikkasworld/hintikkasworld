import { SymbolicPublicAction } from './symbolic-public-action';

describe('SymbolicPublicAction', () => {
  it('should create an instance', () => {
    expect(new SymbolicPublicAction(null,null)).toBeTruthy();
  });
});
