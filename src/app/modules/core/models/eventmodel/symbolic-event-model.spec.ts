import { SymbolicEventModel } from './symbolic-event-model';

describe('SymbolicEventModel', () => {
  it('should create an instance', () => {
    expect(new SymbolicEventModel()).toBeTruthy();
  });
});
