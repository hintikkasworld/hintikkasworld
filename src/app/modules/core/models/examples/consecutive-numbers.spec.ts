import { ConsecutiveNumbers } from './consecutive-numbers';

describe('ConsecutiveNumbers', () => {
  it('should create an instance', () => {
    expect(new ConsecutiveNumbers()).toBeTruthy();
  });
});
