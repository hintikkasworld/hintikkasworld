import { CoordinatedAttackProblem } from './coordinated-attack-problem';

describe('CoordinatedAttackProblem', () => {
    it('should create an instance', () => {
        expect(new CoordinatedAttackProblem()).toBeTruthy();
    });
});
